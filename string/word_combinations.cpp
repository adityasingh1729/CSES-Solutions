#include <iostream>
#include <algorithm>
#include <numeric>
#include <random>
#include <string>
#include <map>
#include <unordered_map>
#include <vector>
#include <queue>
#include <set>
#include <unordered_set>
#include <cmath>
#include <chrono>
 
using namespace std;

template <typename T>
void printVector(vector<T> vec) {
  cout << "size = " << vec.size() << ": ";
  for (auto &elem: vec) {
    cout << elem << " ";
  }
  cout << endl;
}

void solve()
{
    int MOD = pow(10,9) + 7;
    
}

int main()
{
    string s; cin >> s;
    int k; cin >> k;
    vector<int> dict; dict.reserve(k);

    return 0;
}